{
  "name": "@acme/sst-bootstrap",
  "private": true,
  "type": "module",
  "scripts": {
    "env:pull": "node ./scripts/pull-secret-env.mjs",
    "env:push": "node ./scripts/push-secret-env.mjs",
    "sst:install": "pnpm with-env sst install",
    "typecheck": "tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "devDependencies": {
    "@acme/env": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@aws-sdk/client-secrets-manager": "^3.1090.0",
    "@types/node": "catalog:",
    "sst": "4.17.1",
    "typescript": "catalog:"
  }
}
