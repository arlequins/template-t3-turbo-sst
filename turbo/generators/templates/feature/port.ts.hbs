import type {
  {{ pascalCase name }}Input,
  {{ pascalCase name }}Result,
} from "../../domain/{{ name }}";

export type {{ pascalCase name }}Port = {
  execute(input: {{ pascalCase name }}Input): Promise<{{ pascalCase name }}Result>;
};
