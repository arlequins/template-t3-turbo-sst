/**
 * `NEXT_PUBLIC_*` fallbacks — no `serverEnv` / `createEnv`.
 * Import from `@acme/env/public-defaults` in Next.js client code to avoid pulling server env.
 */
export const DEFAULT_LOCALHOST_SITE_URL = "http://localhost:3000";
export const DEFAULT_LOCALHOST_API_URL = "http://localhost:5000";
