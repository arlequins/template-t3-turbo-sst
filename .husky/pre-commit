#!/usr/bin/env sh
set -e

echo "pre-commit: lint:fix"
pnpm lint:fix

echo "pre-commit: format:fix"
pnpm format:fix

# Include auto-fixes in this commit (tracked files only)
git add -u

echo "pre-commit: typecheck"
pnpm typecheck
