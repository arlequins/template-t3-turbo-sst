{
  "name": "template-t3-turbo-sst",
  "private": true,
  "engines": {
    "node": "^24.14.0",
    "pnpm": "^10.19.0"
  },
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo run clean",
    "db:pull": "turbo -F @acme/db pull",
    "db:push": "turbo -F @acme/db push",
    "db:create-migration": "turbo -F @acme/db create-migration",
    "db:migrate": "turbo -F @acme/db migrate",
    "db:seed": "turbo -F @acme/db seed",
    "db:studio": "turbo -F @acme/db studio",
    "dev": "turbo watch dev --continue",
    "dev:next": "turbo watch dev -F @acme/web...",
    "dev:sst": "turbo run sst:dev --continue -F @acme/web -F @acme/api",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "turbo run format --continue -- --write --cache --cache-location .cache/.prettiercache",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "lint:ws": "pnpm dlx sherif@latest",
    "postinstall": "pnpm lint:ws",
    "typecheck": "turbo run typecheck",
    "ui-add": "turbo run ui-add",
    "sst:ws": "node ./scripts/sst-workspace.mjs",
    "env:pull": "pnpm -F @acme/sst-bootstrap env:pull",
    "env:push": "pnpm -F @acme/sst-bootstrap env:push"
  },
  "devDependencies": {
    "@acme/prettier-config": "workspace:*",
    "@turbo/gen": "^2.9.5",
    "dotenv-cli": "^11.0.0",
    "prettier": "catalog:",
    "turbo": "^2.9.5",
    "typescript": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
